type Session {
    id: ID! @id
    timeStart: DateTime!
    timeEnd: DateTime!
    locations: [Location]!
    mood: Float
    journal: String
  }

  type User {
    id: ID! @id
    email: String!
    password: String!
  }

  type Location {
    id: ID! @id
    title: String!
    address: String!
    features: [Feature]!
    boundaries: [GeoPoint]!
  }

  type Feature {
    id: ID! @id
    title: String!
  }

  type GeoPoint {
    id: ID! @id
    boundary: Json!
  }

  type Goal {
    id : ID! @id
    hours: Int!
    days: [Days]
  }

  type Days {
    id: ID! @id
    title: String!
  }

  type Progress {
    id: ID! @id
    duration: Float!
    completion: Float!
  }